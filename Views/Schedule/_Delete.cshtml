@model LaLlamaDelBosque.Models.Schedule

<div>
    <partial>
        @Html.Partial("_Details", Model)
    </partial>
    <h6>¿Desea eliminar este Horario?</h6>

    <form asp-action="Delete" asp-route-id=@Model.Id>
        <input type="submit" value="Eliminar" class="btn btn-danger" />
    </form>
</div>
